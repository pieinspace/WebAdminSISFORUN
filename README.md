# RunTracking Backend (starter)

Minimal Express + TypeScript starter for the RunTracking admin.

Quick start

Install dependencies inside `server/`:

```bash
cd server
npm install
npm run dev
```

API endpoints

- `GET /` health
- `GET /api/runners` sample data
